apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dmhub-mgr.fullname" . }}-test-connection"
  labels:
    {{- include "dmhub-mgr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dmhub-mgr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
