apiVersion: v1
kind: Service
metadata:
  name: {{ include "dmhub-ui.fullname" . }}
  labels:
    {{- include "dmhub-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dmhub-ui.selectorLabels" . | nindent 4 }}
