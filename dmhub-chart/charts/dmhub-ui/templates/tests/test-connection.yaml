apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dmhub-ui.fullname" . }}-test-connection"
  labels:
    {{- include "dmhub-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dmhub-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
