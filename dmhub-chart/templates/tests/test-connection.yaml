apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dmhub-charts.fullname" . }}-test-connection"
  labels:
    {{- include "dmhub-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dmhub-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
